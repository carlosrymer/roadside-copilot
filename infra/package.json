{
  "name": "roadside-copilot-infra",
  "version": "0.1.0",
  "private": true,
  "description": "AWS CDK app + Lambda handlers for the Roadside Co-Pilot API",
  "bin": { "roadside-copilot": "bin/app.js" },
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth",
    "deploy": "cdk deploy --require-approval never",
    "destroy": "cdk destroy --force",
    "diff": "cdk diff"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.9.0",
    "aws-cdk": "^2.170.0",
    "esbuild": "^0.24.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.2"
  }
}
